# Database Configuration
# Create a PostgreSQL database and user, then add credentials here
DB_USER=congress_user
DB_HOST=localhost
DB_NAME=congress_data
DB_PASSWORD=your_secure_password_here
DB_PORT=5432

# Congress.gov API Configuration
# Get your free API key from: https://api.congress.gov/
CONGRESS_API_KEY=your_congress_api_key_here
CURRENT_CONGRESS=119

# Server Configuration
PORT=3001

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit the .env file to git (it's in .gitignore for safety)